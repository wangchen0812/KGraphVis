# 🧾 Git 操作快速命令表 + 常见问题速查手册（中文）

适用于你的项目：`D:\研究生的自我修养\红色人物知识图谱构建\WebGraph`

---

## 🎯 一、Git 常用命令速查表

| 功能 | 命令 | 说明 |
|------|------|------|
| 初始化仓库 | `git init` | 创建 `.git` 版本控制目录 |
| 查看状态 | `git status` | 查看文件修改、新增、删除情况 |
| 添加文件 | `git add .` | 添加所有修改到暂存区 |
| 提交更改 | `git commit -m "提交说明"` | 提交修改并附加描述 |
| 查看历史 | `git log --oneline` | 查看提交记录（简洁） |
| 查看差异 | `git diff` | 查看修改内容对比 |
| 推送更新 | `git push` | 上传到 GitHub |
| 拉取更新 | `git pull origin main` | 从远程同步代码 |
| 克隆仓库 | `git clone 仓库地址` | 下载 GitHub 项目 |
| 添加远程 | `git remote add origin 地址` | 绑定远程仓库 |
| 查看远程 | `git remote -v` | 查看当前远程仓库地址 |

---

## 🧠 二、同步本地改动到 GitHub

```bash
cd "D:/研究生的自我修养/红色人物知识图谱构建/WebGraph"
git status
git add .
git commit -m "更新说明，如：优化界面或修复bug"
git push
```

---

## 🧩 三、第一次推送到远程仓库

```bash
git remote add origin git@github.com:wangchen0812/KGraphVis.git
git branch -M main
git push -u origin main
```

---

## 🧰 四、常见问题与解决方法

| 问题 | 原因 | 解决方法 |
|------|------|-----------|
| `Permission denied (publickey)` | 没配置 SSH 公钥 | 生成 SSH key 并添加到 GitHub |
| `Updates were rejected` | 远程与本地不同步 | 执行 `git pull origin main --allow-unrelated-histories` |
| 文件过大警告 | GitHub 限制单文件 >50MB | 使用 Git LFS 或外部存储 |
| `nothing to commit` | 没有修改 | 当前无文件变动 |
| 想撤销提交 | 提交错误 | `git reset --soft HEAD~1` 保留修改 |

---

## 🧾 五、.gitignore 示例

```
# Node
node_modules/
dist/
.env

# Python
__pycache__/
venv/
*.pyc

# IDE / OS
.vscode/
.idea/
.DS_Store
*.log

# 大文件
*.mp4
*.zip
*.csv
```

---

## ⚙️ 六、维护命令

| 命令 | 说明 |
|------|------|
| `git clean -fd` | 删除未跟踪文件 |
| `git rm --cached 文件` | 从仓库移除但保留本地 |
| `git stash` / `git stash pop` | 暂存和恢复修改 |
| `git reflog` | 查看 HEAD 历史 |

---

## 🧭 七、提交信息命名规范

| 类型 | 含义 | 示例 |
|------|------|------|
| `feat:` | 新功能 | `feat: 新增时间轴组件` |
| `fix:` | 修复 bug | `fix: 修复后端 Neo4j 查询错误` |
| `refactor:` | 优化代码结构 | `refactor: 优化 Flask 路由` |
| `style:` | 样式调整 | `style: 调整前端间距` |
| `docs:` | 文档更新 | `docs: 更新 README` |

---

## ✅ 八、快速同步口诀

> 查状态 → 加改动 → 写说明 → 推上去

```bash
git status
git add .
git commit -m "修改说明"
git push
```

---

本手册编写：ChatGPT 自动生成，用于项目版本管理指导。
